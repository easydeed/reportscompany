/**
 * Layout for branding preview pages.
 * No shell, no nav - just raw HTML for PDF generation.
 */
export default function BrandingPreviewLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

