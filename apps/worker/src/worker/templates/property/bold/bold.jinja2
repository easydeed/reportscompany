{# =============================================================================
   BOLD THEME
   Confident, powerful, modern aesthetic with gold accents
   
   Typography: Oswald (display) + Inter (body)
   Colors: Deep navy + gold
   Style: Strong geometric shapes, bold type, high contrast
   ============================================================================= #}

{% extends '_base/base.jinja2' %}

{% block fonts %}
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Oswald:wght@400;500;600;700&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
{% endblock %}

{% block theme_styles %}
<style>
  :root {
    /* Bold Theme Colors - Primary is customizable via theme_color */
    --color-primary: {{ theme_color | default('#0F1629') }};
    --color-primary-light: {{ theme_color | default('#1a2744') }};
    --color-accent: #C9A227;
    --color-accent-light: #dbb94a;
    --color-background: #FFFFFF;
    --color-surface: #F8F7F4;
    --color-text: #0F1629;
    --color-text-muted: #64748B;
    --color-border: #E2E8F0;
    
    /* Typography */
    --font-display: 'Oswald', system-ui, sans-serif;
    --font-body: 'Inter', system-ui, sans-serif;
    
    /* Bold-specific */
    --radius-sm: 2px;
    --radius-md: 4px;
    --radius-lg: 6px;
    
    --shadow-sm: 0 2px 4px rgba(15, 22, 41, 0.08);
    --shadow-md: 0 4px 12px rgba(15, 22, 41, 0.12);
    --shadow-lg: 0 12px 40px rgba(15, 22, 41, 0.18);
  }
  
  /* ==========================================================================
     BOLD: Cover Page
     ========================================================================== */
  
  .page-cover {
    background: var(--color-primary);
  }
  
  .page-cover .cover-bg {
    position: absolute;
    inset: 0;
    background: 
      linear-gradient(180deg, 
        rgba(15, 22, 41, 0.05) 0%,
        rgba(15, 22, 41, 0.3) 40%,
        rgba(15, 22, 41, 0.92) 100%
      ){% if images.hero %},
      url('{{ images.hero }}') center/cover no-repeat{% endif %};
  }
  
  /* Top accent bar */
  .page-cover::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 6px;
    background: linear-gradient(90deg, var(--color-accent) 0%, var(--color-accent) 40%, var(--color-primary) 40%);
    z-index: 10;
  }
  
  /* Branding header */
  .page-cover .cover-branding-header {
    position: absolute;
    top: var(--page-margin);
    left: var(--page-margin);
    right: var(--page-margin);
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    z-index: 5;
  }
  
  .page-cover .logo-mark {
    width: 48px;
    height: 48px;
    background: var(--color-accent);
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 18px;
    color: var(--color-primary);
  }
  
  .page-cover .report-badge {
    background: rgba(255,255,255,0.95);
    padding: 10px 20px;
    font-family: var(--font-display);
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--color-primary);
  }
  
  /* Main content area */
  .page-cover .cover-content {
    position: absolute;
    bottom: 140px;
    left: var(--page-margin);
    right: var(--page-margin);
    z-index: 5;
  }
  
  .page-cover .cover-property-type {
    font-family: var(--font-display);
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--color-accent);
    margin-bottom: 12px;
  }
  
  .page-cover .cover-address {
    font-family: var(--font-display);
    font-size: 52px;
    font-weight: 700;
    line-height: 1.05;
    color: white;
    text-transform: uppercase;
    letter-spacing: -1px;
    margin-bottom: 16px;
  }
  
  .page-cover .cover-location {
    font-family: var(--font-body);
    font-size: 16px;
    font-weight: 500;
    color: rgba(255,255,255,0.8);
    letter-spacing: 0.5px;
  }
  
  .page-cover .cover-accent-line {
    width: 80px;
    height: 4px;
    background: var(--color-accent);
    margin: 24px 0;
  }
  
  .page-cover .property-stats {
    margin-top: 20px;
  }
  
  .page-cover .stat-value {
    color: white;
    font-size: 28px;
  }
  
  .page-cover .stat-label {
    color: rgba(255,255,255,0.6);
  }
  
  /* Corner accent */
  .page-cover::after {
    content: '';
    position: absolute;
    bottom: 110px;
    right: 0;
    width: 120px;
    height: 120px;
    background: var(--color-accent);
    clip-path: polygon(100% 0, 100% 100%, 0 100%);
    opacity: 0.9;
    z-index: 4;
  }
  
  /* Footer override for cover */
  .page-cover .cover-footer {
    height: 110px;
    padding: 0 var(--page-margin);
    border-top: none;
  }
  
  .page-cover .cover-agent-photo {
    border-color: var(--color-accent);
    width: 64px;
    height: 64px;
  }
  
  .page-cover .cover-agent-name {
    font-size: 18px;
    font-weight: 700;
  }
  
  /* ==========================================================================
     BOLD: Page Header Overrides
     ========================================================================== */
  
  .page-header {
    border-bottom: 3px solid var(--color-primary);
  }
  
  .page-title {
    text-transform: uppercase;
    letter-spacing: 1px;
    font-size: 32px;
  }
  
  .page-header-accent {
    display: none; /* Using border instead */
  }
  
  /* ==========================================================================
     BOLD: Contents Page
     ========================================================================== */
  
  .page-contents .contents-item {
    border-bottom: 2px solid var(--color-border);
    padding: var(--space-md) 0;
  }
  
  .page-contents .contents-number {
    font-size: 18px;
    font-weight: 700;
    color: var(--color-accent);
    min-width: 40px;
  }
  
  .page-contents .contents-title {
    text-transform: uppercase;
    letter-spacing: 0.5px;
    font-size: 18px;
  }
  
  /* ==========================================================================
     BOLD: Data Tables
     ========================================================================== */
  
  .data-table tr:nth-child(odd) {
    background: var(--color-surface);
  }
  
  .data-table td {
    padding: 12px 16px;
  }
  
  .table-title {
    background: var(--color-primary);
    color: white;
    padding: 12px 16px;
    margin-bottom: 0;
  }
  
  /* ==========================================================================
     BOLD: Comp Cards
     ========================================================================== */
  
  .comp-card {
    border: 2px solid var(--color-border);
    border-radius: var(--radius-md);
  }
  
  .comp-card-header {
    background: var(--color-primary);
    padding: 12px var(--space-md);
  }
  
  .comp-price {
    font-size: 18px;
    letter-spacing: -0.5px;
  }
  
  .comp-address {
    text-transform: uppercase;
    letter-spacing: 0.3px;
  }
  
  /* ==========================================================================
     BOLD: Analysis Summary
     ========================================================================== */
  
  .analysis-summary {
    background: var(--color-surface);
    border: 2px solid var(--color-border);
    border-radius: var(--radius-md);
  }
  
  .range-value {
    letter-spacing: -1px;
  }
  
  .range-median {
    background: var(--color-primary);
    color: white;
    padding: var(--space-md);
    margin: calc(-1 * var(--space-lg));
    margin-bottom: var(--space-lg);
  }
  
  .range-median .range-label {
    color: rgba(255,255,255,0.7);
  }
  
  .range-median .range-value {
    color: var(--color-accent);
    font-size: 28px;
  }
  
  /* ==========================================================================
     BOLD: Map Card
     ========================================================================== */
  
  .map-card {
    border: 2px solid var(--color-border);
    border-radius: var(--radius-md);
  }
  
  .map-card-header {
    background: var(--color-primary);
    color: white;
    text-transform: uppercase;
    letter-spacing: 1px;
  }
  
  .map-card-image {
    height: 450px;
  }
  
  /* ==========================================================================
     BOLD: Price Range
     ========================================================================== */
  
  .price-range-track {
    height: 12px;
    background: linear-gradient(90deg, 
      var(--color-surface) 0%, 
      var(--color-accent) 35%,
      var(--color-primary) 65%,
      var(--color-surface) 100%
    );
    border-radius: 2px;
  }
  
  .price-range-marker-label {
    background: var(--color-primary);
    font-size: 14px;
    padding: 6px 14px;
  }
  
  /* Range comps list */
  .range-comps-list {
    margin-top: var(--space-xl);
    background: var(--color-surface);
    border: 2px solid var(--color-border);
  }
  
  .range-comp-item {
    display: flex;
    justify-content: space-between;
    padding: var(--space-md) var(--space-lg);
    border-bottom: 1px solid var(--color-border);
  }
  
  .range-comp-item:last-child {
    border-bottom: none;
  }
  
  .range-comp-item:nth-child(odd) {
    background: white;
  }
  
  .range-comp-address {
    font-family: var(--font-display);
    font-size: 14px;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.3px;
  }
  
  .range-comp-price {
    font-family: var(--font-display);
    font-size: 16px;
    font-weight: 700;
    color: var(--color-primary);
  }
  
  /* ==========================================================================
     BOLD: Footer Overrides
     ========================================================================== */
  
  .page-footer {
    border-top: 3px solid var(--color-primary);
  }
  
  .footer-agent-name {
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }
  
  /* V0 overrides — Bold */
  .section-title {
    text-transform: uppercase;
    letter-spacing: 1.5px;
  }
  
  .comparison-header {
    background: var(--color-primary);
  }
  
  .range-bar-track {
    background: linear-gradient(90deg, var(--color-surface) 0%, var(--color-accent) 100%);
  }
  
  .market-insight-title {
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  /* ==========================================================================
     BOLD: Internal Page Accents — Theme personality on pages 2-7
     ========================================================================== */

  /* Strong accent bar on internal pages */
  .page-contents::before,
  .page-aerial::before,
  .page-property::before,
  .page-analysis::before,
  .page-comparables::before,
  .page-range::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 6px;
    background: linear-gradient(90deg, var(--color-accent) 0%, var(--color-accent) 40%, var(--color-primary) 40%);
    z-index: 10;
  }

  /* Key stats bar */
  .key-stats-bar {
    background: var(--color-primary);
  }

  /* Analysis summary — bold border */
  .analysis-summary {
    border-top: 4px solid var(--color-primary);
  }

  /* Range bar container */
  .range-bar-container {
    border-top: 4px solid var(--color-accent);
  }

  /* Stats grid cards with gold accent */
  .range-stat-card {
    border-top: 3px solid var(--color-accent);
  }

  /* Market insight */
  .market-insight {
    border-left: 4px solid var(--color-accent);
    background: var(--color-surface);
  }

  /* Comp cards — gold accent line */
  .comp-card {
    border-top: 3px solid var(--color-accent);
  }

  /* Contents sidebar accent */
  .page-contents .contents-sidebar-accent {
    width: 4px;
    background: var(--color-accent);
    opacity: 0.7;
  }

  /* ==========================================================================
     BOLD: Per-Theme Page Overrides — Analysis, Comparables, Range
     ========================================================================== */

  /* --- Analysis Page --- */
  .bold-section-header {
    background: var(--color-primary);
    padding: 10px 16px;
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .bold-section-header h3 {
    font-family: var(--font-display);
    font-size: 13px;
    font-weight: 600;
    color: white;
    text-transform: uppercase;
    letter-spacing: 1.5px;
    margin: 0;
  }

  .bold-gold-bar {
    width: 4px;
    height: 18px;
    background: var(--color-accent);
  }

  .bold-stats-row {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 10px;
    margin-bottom: 14px;
  }

  .bold-stat-box {
    background: var(--color-primary);
    padding: 14px 10px;
    text-align: center;
    border-left: 3px solid var(--color-accent);
  }

  .bold-stat-box .num {
    font-family: var(--font-display);
    font-size: 22px;
    font-weight: 700;
    color: var(--color-accent);
    line-height: 1;
  }

  .bold-stat-box .lbl {
    font-size: 8px;
    color: rgba(255,255,255,0.7);
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-top: 4px;
  }

  .bold-analysis-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 11px;
    border: 2px solid var(--color-primary);
  }

  .bold-analysis-table thead {
    background: var(--color-primary);
    color: white;
  }

  .bold-analysis-table th {
    padding: 10px 10px;
    text-align: left;
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  .bold-analysis-table tbody tr:nth-child(odd) {
    background: var(--color-surface);
  }

  .bold-analysis-table td {
    padding: 8px 10px;
    border-bottom: 1px solid var(--color-border);
  }

  .bold-analysis-table tfoot {
    background: var(--color-accent);
    color: var(--color-primary);
    font-weight: 700;
  }

  .bold-analysis-table tfoot td {
    padding: 10px;
    font-family: var(--font-display);
    font-size: 12px;
  }

  .bold-divider {
    height: 4px;
    background: linear-gradient(90deg, var(--color-accent) 40%, var(--color-primary) 40%);
    margin: 14px 0;
  }

  .bold-position-bar {
    background: var(--color-surface);
    border: 2px solid var(--color-primary);
    padding: 14px 16px;
    margin-top: 10px;
  }

  .bold-position-bar .track {
    height: 14px;
    background: linear-gradient(90deg, var(--color-surface) 0%, var(--color-accent) 35%, var(--color-primary) 65%, var(--color-surface) 100%);
    position: relative;
    margin: 12px 0 6px;
  }

  .bold-position-bar .marker {
    position: absolute;
    top: -22px;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .bold-position-bar .marker-label {
    font-family: var(--font-display);
    font-size: 10px;
    font-weight: 700;
    color: var(--color-primary);
    white-space: nowrap;
  }

  .bold-position-bar .marker-tick {
    width: 3px;
    height: 28px;
    background: var(--color-primary);
  }

  .bold-position-bar .ends {
    display: flex;
    justify-content: space-between;
    font-size: 9px;
    color: var(--color-text-muted);
  }

  /* --- Comparables Page --- */
  .bold-comp-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 14px;
    margin-top: 16px;
  }

  .bold-comp-card {
    border: 2px solid var(--color-primary);
    overflow: hidden;
    position: relative;
  }

  .bold-comp-card-header {
    background: var(--color-primary);
    padding: 8px 14px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 3px solid var(--color-accent);
  }

  .bold-comp-card-header .comp-num {
    font-family: var(--font-display);
    font-size: 11px;
    font-weight: 600;
    color: rgba(255,255,255,0.6);
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  .bold-comp-card-header .comp-price {
    font-family: var(--font-display);
    font-size: 18px;
    font-weight: 700;
    color: var(--color-accent);
  }

  .bold-comp-img {
    height: 100px;
    background: var(--color-surface);
    position: relative;
    overflow: hidden;
  }

  .bold-comp-img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .bold-comp-triangle {
    position: absolute;
    bottom: 0;
    right: 0;
    width: 0;
    height: 0;
    border-style: solid;
    border-width: 0 0 30px 30px;
    border-color: transparent transparent var(--color-accent) transparent;
  }

  .bold-comp-body {
    padding: 10px 14px;
  }

  .bold-comp-address {
    font-family: var(--font-display);
    font-size: 12px;
    font-weight: 600;
    color: var(--color-primary);
    text-transform: uppercase;
    letter-spacing: 0.3px;
    margin-bottom: 8px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .bold-comp-stats {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 6px;
  }

  .bold-comp-stat {
    background: var(--color-surface);
    padding: 6px 8px;
    text-align: center;
  }

  .bold-comp-stat .lbl {
    font-size: 8px;
    color: var(--color-text-muted);
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin-bottom: 2px;
  }

  .bold-comp-stat .val {
    font-family: var(--font-display);
    font-size: 12px;
    font-weight: 600;
    color: var(--color-primary);
  }

  /* --- Range Page --- */
  .bold-range-stats {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 10px;
    margin-bottom: 14px;
  }

  .bold-range-stat {
    background: var(--color-primary);
    padding: 16px 8px;
    text-align: center;
  }

  .bold-range-stat .num {
    font-family: var(--font-display);
    font-size: 28px;
    font-weight: 700;
    color: var(--color-accent);
    line-height: 1;
  }

  .bold-range-stat .lbl {
    font-size: 8px;
    color: rgba(255,255,255,0.7);
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-top: 6px;
  }

  .bold-range-slider {
    background: var(--color-surface);
    border: 2px solid var(--color-accent);
    padding: 20px;
    margin-bottom: 14px;
  }

  .bold-range-slider-header {
    display: flex;
    justify-content: space-between;
    margin-bottom: 12px;
  }

  .bold-range-slider-price {
    font-family: var(--font-display);
    font-size: 22px;
    font-weight: 700;
    color: var(--color-primary);
  }

  .bold-range-track {
    height: 12px;
    background: linear-gradient(90deg, var(--color-accent) 0%, var(--color-primary) 50%, var(--color-accent) 100%);
    position: relative;
    margin-bottom: 12px;
  }

  .bold-range-dot {
    width: 18px;
    height: 18px;
    background: white;
    border: 3px solid var(--color-primary);
    position: absolute;
    top: 50%;
    transform: translate(-50%, -50%);
  }

  .bold-range-comps {
    border: 2px solid var(--color-primary);
  }

  .bold-range-comp {
    display: flex;
    align-items: center;
    padding: 8px 14px;
    border-bottom: 1px solid var(--color-border);
    gap: 10px;
  }

  .bold-range-comp:last-child {
    border-bottom: none;
  }

  .bold-range-comp:nth-child(odd) {
    background: var(--color-surface);
  }

  .bold-range-comp .addr {
    flex: 1;
    font-family: var(--font-display);
    font-size: 11px;
    font-weight: 500;
    text-transform: uppercase;
    color: var(--color-primary);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .bold-range-comp .dist {
    font-size: 9px;
    color: var(--color-text-muted);
    min-width: 40px;
    text-align: center;
  }

  .bold-range-comp .price {
    font-family: var(--font-display);
    font-size: 13px;
    font-weight: 700;
    color: var(--color-primary);
    min-width: 80px;
    text-align: right;
  }

  .bold-range-note {
    font-size: 10px;
    line-height: 1.6;
    color: var(--color-text-muted);
    text-align: center;
    margin-top: 10px;
    padding: 12px 16px;
    background: var(--color-surface);
    border-left: 4px solid var(--color-accent);
  }
</style>
{% endblock %}

{# =============================================================================
   COVER PAGE OVERRIDE
   Bold has a unique cover layout
   ============================================================================= #}
{% block cover %}
<div class="cover-bg"></div>

<div class="cover-branding-header">
  {% if agent.logo_url %}
  <img src="{{ agent.logo_url }}" alt="{{ agent.company_name }}" style="max-height: 48px; max-width: 140px; object-fit: contain;">
  {% else %}
  <div class="logo-mark">{{ agent.company_short | default(agent.company_name[:2] | upper) }}</div>
  {% endif %}
  <div class="report-badge">Property Report</div>
</div>

<div class="cover-content">
  <div class="cover-property-type">{{ property.property_type | default('Single Family Residence') }}</div>
  <h1 class="cover-address">{{ property.street_address }}</h1>
  <p class="cover-location">{{ property.city }}, {{ property.state }} {{ property.zip_code }}</p>
  <div class="cover-accent-line"></div>
  {{ macros.property_stats(property) }}
</div>

{{ macros.cover_agent_block(agent) }}
{% endblock %}

{# =============================================================================
   ANALYSIS PAGE OVERRIDE — Bold geometric, high-impact layout
   ============================================================================= #}
{% block analysis %}
<div class="page-content">
  <div class="bold-section-header">
    <div class="bold-gold-bar"></div>
    <h3>Area Sales Analysis</h3>
  </div>

  <div style="margin-top:14px;">
    <div class="bold-stats-row">
      <div class="bold-stat-box">
        <div class="num">{{ stats.total_comps }}</div>
        <div class="lbl">Comps</div>
      </div>
      <div class="bold-stat-box">
        <div class="num">{{ stats.avg_sqft | default(0) | int | format_number }}</div>
        <div class="lbl">Avg Sq Ft</div>
      </div>
      <div class="bold-stat-box">
        <div class="num">{{ stats.avg_beds | default(0) | round(1) }}</div>
        <div class="lbl">Avg Beds</div>
      </div>
      <div class="bold-stat-box">
        <div class="num">{{ stats.avg_price_per_sqft | format_currency }}</div>
        <div class="lbl">Avg $/Sqft</div>
      </div>
    </div>

    <div class="bold-divider"></div>

    <table class="bold-analysis-table">
      <thead>
        <tr><th>Metric</th><th>Subject</th><th>Low</th><th>Medium</th><th>High</th></tr>
      </thead>
      <tbody>
        <tr><td>Distance (mi)</td><td>{{ stats.piq.distance | default('0') }}</td><td>{{ stats.low.distance | default('-') }}</td><td>{{ stats.medium.distance | default('-') }}</td><td>{{ stats.high.distance | default('-') }}</td></tr>
        <tr><td>Living Area (sf)</td><td>{{ stats.piq.sqft | default('-') }}</td><td>{{ stats.low.sqft | default('-') }}</td><td>{{ stats.medium.sqft | default('-') }}</td><td>{{ stats.high.sqft | default('-') }}</td></tr>
        <tr><td>Price/Sq.Ft.</td><td>{{ stats.piq.price_per_sqft | format_currency }}</td><td>{{ stats.low.price_per_sqft | format_currency }}</td><td>{{ stats.medium.price_per_sqft | format_currency }}</td><td>{{ stats.high.price_per_sqft | format_currency }}</td></tr>
        <tr><td>Year Built</td><td>{{ stats.piq.year_built | default('-') }}</td><td>{{ stats.low.year_built | default('-') }}</td><td>{{ stats.medium.year_built | default('-') }}</td><td>{{ stats.high.year_built | default('-') }}</td></tr>
        <tr><td>Lot Size (sf)</td><td>{{ stats.piq.lot_size | format_number }}</td><td>{{ stats.low.lot_size | format_number }}</td><td>{{ stats.medium.lot_size | format_number }}</td><td>{{ stats.high.lot_size | format_number }}</td></tr>
        <tr><td>Bedrooms</td><td>{{ stats.piq.bedrooms | default('-') }}</td><td>{{ stats.low.bedrooms | default('-') }}</td><td>{{ stats.medium.bedrooms | default('-') }}</td><td>{{ stats.high.bedrooms | default('-') }}</td></tr>
        <tr><td>Bathrooms</td><td>{{ stats.piq.bathrooms | default('-') }}</td><td>{{ stats.low.bathrooms | default('-') }}</td><td>{{ stats.medium.bathrooms | default('-') }}</td><td>{{ stats.high.bathrooms | default('-') }}</td></tr>
      </tbody>
      <tfoot>
        <tr><td>Sale Price</td><td>{{ stats.piq.price | format_currency }}</td><td>{{ stats.low.price | format_currency }}</td><td>{{ stats.medium.price | format_currency }}</td><td>{{ stats.high.price | format_currency }}</td></tr>
      </tfoot>
    </table>

    {# Price Position Bar #}
    {% set low = stats.price_low | default(0, true) | float %}
    {% set high = stats.price_high | default(0, true) | float %}
    {% set subject = stats.piq.price | default(0, true) | float %}
    {% if subject == 0 %}{% set subject = stats.medium.price | default(0, true) | float %}{% endif %}
    {% set range = high - low %}
    {% if range > 0 %}
    {% set position = ((subject - low) / range * 100) | int %}
    <div class="bold-position-bar">
      <div style="font-family:var(--font-display);font-size:10px;font-weight:600;text-transform:uppercase;letter-spacing:1.5px;color:var(--color-primary);">Subject Property Position</div>
      <div class="track">
        <div class="marker" style="left:{{ position }}%">
          <span class="marker-label">{{ subject | format_currency }}</span>
          <span class="marker-tick"></span>
        </div>
      </div>
      <div class="ends">
        <span>{{ low | format_currency }}</span>
        <span>{{ high | format_currency }}</span>
      </div>
    </div>
    {% endif %}
  </div>
</div>
{{ macros.page_footer(agent, 5) }}
{% endblock %}

{# =============================================================================
   COMPARABLES PAGE OVERRIDE — Thick borders, gold header bars, corner triangles
   ============================================================================= #}
{% block comparables %}
<div class="page-content">
  <div class="bold-section-header">
    <div class="bold-gold-bar"></div>
    <h3>Sales Comparables</h3>
  </div>

  <div class="bold-comp-grid">
    {% for comp in comparables[:4] %}
    <div class="bold-comp-card">
      <div class="bold-comp-card-header">
        <span class="comp-num">Comp {{ loop.index }}</span>
        <span class="comp-price">{{ comp.sale_price | format_currency }}</span>
      </div>
      <div class="bold-comp-img">
        {% if comp.photo_url or comp.map_image_url %}
        <img src="{{ comp.photo_url | default(comp.map_image_url) }}" alt="{{ comp.address }}">
        {% endif %}
        <div class="bold-comp-triangle"></div>
      </div>
      <div class="bold-comp-body">
        <div class="bold-comp-address">{{ comp.address }}</div>
        <div class="bold-comp-stats">
          <div class="bold-comp-stat"><div class="lbl">Sold</div><div class="val">{{ comp.sold_date }}</div></div>
          <div class="bold-comp-stat"><div class="lbl">Sq.Ft.</div><div class="val">{{ comp.sqft | format_number }}</div></div>
          <div class="bold-comp-stat"><div class="lbl">Bed/Bath</div><div class="val">{{ comp.bedrooms }}/{{ comp.bathrooms }}</div></div>
          <div class="bold-comp-stat"><div class="lbl">$/Sq.Ft.</div><div class="val">{{ comp.price_per_sqft | format_currency }}</div></div>
        </div>
      </div>
    </div>
    {% endfor %}
  </div>
</div>
{{ macros.page_footer(agent, 6) }}
{% endblock %}

{# =============================================================================
   RANGE PAGE OVERRIDE — Geometric bar, navy stat boxes, gold accents
   ============================================================================= #}
{% block range %}
<div class="page-content">
  <div class="bold-section-header">
    <div class="bold-gold-bar"></div>
    <h3>Range of Sales</h3>
  </div>

  <div style="margin-top:14px;">
    <div class="bold-range-stats">
      <div class="bold-range-stat">
        <div class="num">{{ stats.total_comps }}</div>
        <div class="lbl">Total Comps</div>
      </div>
      <div class="bold-range-stat">
        <div class="num">{{ stats.avg_sqft | default(0) | int }}</div>
        <div class="lbl">Avg Sq Ft</div>
      </div>
      <div class="bold-range-stat">
        <div class="num">{{ stats.avg_beds | default(0) | round(1) }}</div>
        <div class="lbl">Avg Beds</div>
      </div>
      <div class="bold-range-stat">
        <div class="num">{{ stats.avg_baths | default(0) | round(1) }}</div>
        <div class="lbl">Avg Baths</div>
      </div>
    </div>

    <div class="bold-range-slider">
      <div class="bold-range-slider-header">
        <div class="bold-range-slider-price">{{ stats.price_low | format_currency_short }}</div>
        <div class="bold-range-slider-price">{{ stats.price_high | format_currency_short }}</div>
      </div>
      <div class="bold-range-track">
        {% set subj_price = stats.piq.price | default(0, true) | float %}
        {% set r_low = stats.price_low | default(0, true) | float %}
        {% set r_high = stats.price_high | default(0, true) | float %}
        {% if r_high != r_low and subj_price > 0 %}
        {% set pct = ((subj_price - r_low) / (r_high - r_low) * 100) | int %}
        <div class="bold-range-dot" style="left:{{ pct }}%;"></div>
        {% endif %}
      </div>
    </div>

    <div class="bold-divider"></div>

    <div class="bold-range-comps">
      {% for comp in comparables[:4] | sort(attribute='sale_price') %}
      <div class="bold-range-comp">
        <span class="addr">{{ comp.address }}</span>
        <span class="dist">{{ comp.distance_miles }} mi</span>
        <span class="price">{{ comp.sale_price | format_currency }}</span>
      </div>
      {% endfor %}
    </div>

    <div class="bold-range-note">
      Based on {{ stats.total_comps }} comparable sales, the estimated market value falls between {{ stats.price_low | format_currency }} and {{ stats.price_high | format_currency }}. The median comparable sold at {{ stats.medium.price | format_currency }}.
    </div>
  </div>
</div>
{{ macros.page_footer(agent, 7) }}
{% endblock %}
